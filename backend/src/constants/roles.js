/**
 * Backend constants — Roles
 * Must match frontend constants/roles.js
 */

export const ROLES = {
  CITIZEN: 'citizen',
  AUTHORITY: 'authority',
  POLICE: 'police',
  ADMIN: 'admin',
};

export const ALL_ROLES = Object.values(ROLES);
